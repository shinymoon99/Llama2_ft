<t>配置</t><c>Voice VLAN的802.1p和DSCP优先级</c>。
<t>配置</t><c>不同的过滤器进行路由过滤</c>。
<t>配置</t><c>AGG ASBR和Core ASBR</c>。
<t>配置</t><c>设备全局发送LLDP报文的周期</c>。
（可选）<t>配置</t><c>设备禁止向上游路由器发送IGMP Query报文</c>。
<t>配置</t><c>FTP服务器的IP地址、用户名、密码及工作目录</c>。
<t>配置</t><c>接口允许通过的VLAN</c>，取值范围是1～4094。
<t>配置</t><c>BGP对从全局接收的路由进行过滤</c>。
<t>配置</t><c>simple加密方式认证</c>。
<t>配置</t><c>设备与控制器建立连接时使用的对接密码</c>。
<t>配置</t><c>接口加入的缺省VLAN</c>，取值范围为1～4094。
<t>配置</t><c>Trunk类型接口的缺省VLAN</c>，取值范围是1～4094。
<t>配置</t><c>Keychain认证</c>。
为模板<t>配置</t><c>AS接口入方向上DHCP报文的限速值</c>。
<t>配置</t><c>接口的链路类型为Hybrid</c>。
<t>配置</t><c>新的rule规则</c>，并删除不用的rule规则。
（可选）<t>配置</t><c>在邻居状态由Down到Up时将BGP4+路由的优先级调整为最低优先级</c>。
根据需要，<t>配置</t><c>PIM剪枝控制参数</c>。
（可选）<t>配置</t><c>设备重启后将BGP4+路由延迟发布</c>。
<t>配置</t><c>接口的LLDP工作模式</c>。
<t>配置</t><c>模板中未知单播流量抑制功能的包速率</c>。
（可选）<t>配置</t><c>BGP4+内存保护</c>。
（可选）<t>配置</t><c>BGP把发布给邻居的路由改为最低优先级</c>。
在SwitchB<t>配置</t><c>两条IP静态路由</c>。
（可选）<t>配置</t><c>BGP内存保护</c>。
<t>配置</t><c>直连方式多主检测</c>。
<t>配置</t><c>接口以untagged方式允许缺省VLAN通过</c>。
<t>配置</t><c>Join/Prune报文中Join信息的合法源地址范围</c>。
<t>配置</t><c>安全MAC地址的限制数</c>，取值范围为1～1024。