配置Voice VLAN的802.1p和DSCP优先级。
配置不同的过滤器进行路由过滤。
配置AGG ASBR和Core ASBR。
配置设备全局发送LLDP报文的周期。
（可选）配置设备禁止向上游路由器发送IGMP Query报文。
配置FTP服务器的IP地址、用户名、密码及工作目录。
配置接口允许通过的VLAN，取值范围是1～4094。
配置BGP对从全局接收的路由进行过滤。
配置simple加密方式认证。
配置设备与控制器建立连接时使用的对接密码。
配置接口加入的缺省VLAN，取值范围为1～4094。
配置Trunk类型接口的缺省VLAN，取值范围是1～4094。
配置Keychain认证。
为模板配置AS接口入方向上DHCP报文的限速值。
配置接口的链路类型为Hybrid。
配置新的rule规则，并删除不用的rule规则。
（可选）配置在邻居状态由Down到Up时将BGP4+路由的优先级调整为最低优先级。
根据需要，配置PIM剪枝控制参数。
（可选）配置设备重启后将BGP4+路由延迟发布。
配置接口的LLDP工作模式。
配置模板中未知单播流量抑制功能的包速率。
（可选）配置BGP4+内存保护。
（可选）配置BGP把发布给邻居的路由改为最低优先级。
在SwitchB配置两条IP静态路由。
（可选）配置BGP内存保护。
配置直连方式多主检测。
配置接口以untagged方式允许缺省VLAN通过。
配置Join/Prune报文中Join信息的合法源地址范围。
配置安全MAC地址的限制数，取值范围为1～1024。